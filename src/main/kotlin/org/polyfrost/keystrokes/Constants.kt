package org.polyfrost.keystrokes

private const val CB_HEIGHT = 16

private const val NORMAL_HEIGHT = 19

val CB_LAYOUT = arrayOf(
    intArrayOf(CB_HEIGHT, 6),  // W
    intArrayOf(CB_HEIGHT, 23), // A
    intArrayOf(CB_HEIGHT, 23), // S
    intArrayOf(CB_HEIGHT, 23)  // D
)

val NORMAL_LAYOUT = arrayOf(
    intArrayOf(NORMAL_HEIGHT, 0),  // W
    intArrayOf(NORMAL_HEIGHT, 20), // A
    intArrayOf(NORMAL_HEIGHT, 20), // S
    intArrayOf(NORMAL_HEIGHT, 20)  // D
)